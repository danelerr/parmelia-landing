---
import { getLang } from '../i18n';

const lang = getLang(Astro.currentLocale);
const t = {
  es: {
    title: 'Una parte disponible. Otra, creciendo.',
    copy: 'Tú decides cuánto USDC queda listo para usar y cuánto pasa a Creciendo con Aave V3. Cuando lo necesites, puedes retirarlo para volver a usarlo.',
    micro: 'La tasa es variable y existen riesgos. Aave, la red, el contrato y la salida permanecen visibles.',
    points: [
      'Eliges cuánto USDC mover a Creciendo.',
      'Ves la tasa y los riesgos antes de confirmar.',
      'Puedes retirarlo para volver a dejarlo Disponible.',
    ],
  },
  en: {
    title: 'Part available. Part growing.',
    copy: 'You decide how much USDC stays ready to use and how much moves to Growing through Aave V3. When needed, you can withdraw it to use it again.',
    micro: 'The rate is variable and risks exist. Aave, the network, the contract, and the exit route remain visible.',
    points: [
      'You choose how much USDC to move into Growing.',
      'You see the rate and risks before confirming.',
      'You can withdraw it to make it Available again.',
    ],
  },
}[lang];
const points = t.points;
---

<section id="earn" class="landing-section earn-gradient">
  <div class="landing-shell">
    <div class="copy-stack" data-reveal>
      <h2 class="section-title">{t.title}</h2>
      <p class="section-copy">{t.copy}</p>
    </div>

    <div class="earn-panel" data-reveal>
      <svg class="earn-orbit" viewBox="0 0 200 230" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <!-- rising yield -->
        <circle class="gas-particle" cx="100" cy="62" r="6" fill="#EFE08C" />
        <circle class="gas-particle" cx="76" cy="74" r="4" fill="#9CE3F4" />
        <circle class="gas-particle" cx="124" cy="70" r="5" fill="#F4A9CF" />
        <!-- growing stack -->
        <g transform="translate(0, 34)">
          <polygon class="stake-layer" points="100,120 170,155 100,190 30,155" fill="rgba(255,255,255,.1)" />
          <polygon class="stake-layer" points="100,90 170,125 100,160 30,125" fill="#A7D4DE" />
          <polygon class="stake-layer" points="100,60 170,95 100,130 30,95" fill="#DEA6BC" />
          <polygon class="stake-layer" points="100,30 170,65 100,100 30,65" fill="#DED9A6" />
        </g>
      </svg>

      <div class="earn-copy">
        {points.map((point, index) => (
          <div class="earn-point">
            <span>{String(index + 1).padStart(2, '0')}</span>
            <p>{point}</p>
          </div>
        ))}
      </div>
    </div>

    <p class="microcopy mt-10" data-reveal>{t.micro}</p>
  </div>
</section>
