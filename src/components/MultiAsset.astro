---
import TokenIcon from './TokenIcon.astro';
import { getLang } from '../i18n';

const lang = getLang(Astro.currentLocale);
const t = {
  es: {
    title: 'USDC para el día a día. Web3 cuando lo buscas.',
    copy: 'Tu dinero principal se organiza en dólares digitales. ETH y otras posiciones aparecen solo cuando forman parte de lo que haces.',
    micro: 'USDC primero no significa USDC solamente: significa una experiencia que empieza por lo útil.',
    desc: {
      USDC: 'Para cobrar, pagar y mantener valor estable.',
      ETH: 'Para moverte por la red cuando lo necesites.',
    },
  },
  en: {
    title: 'USDC for everyday money. Web3 when you look for it.',
    copy: 'Your main money is organized in digital dollars. ETH and other positions appear only when they are part of what you do.',
    micro: 'USDC first does not mean USDC only. It means an experience that starts with what is useful.',
    desc: {
      USDC: 'To charge, pay and hold stable value.',
      ETH: 'To move around the network whenever you need.',
    },
  },
}[lang];

const assets = [
  { token: 'USDC', name: 'USDC', desc: t.desc.USDC },
  { token: 'ETH', name: 'ETH', desc: t.desc.ETH },
] as const;
---

<section id="multi-asset" class="landing-section">
  <div class="landing-shell">
    <div class="copy-stack" data-reveal>
      <h2 class="section-title">{t.title}</h2>
      <p class="section-copy">{t.copy}</p>
    </div>

    <div class="feature-grid" data-reveal-stagger>
      {assets.map((asset) => (
        <article class="feature-card asset-card">
          <TokenIcon token={asset.token} />
          <h3 class="card-title">{asset.name}</h3>
          <p class="card-copy">{asset.desc}</p>
        </article>
      ))}
    </div>

    <p class="microcopy mt-10" data-reveal>{t.micro}</p>
  </div>
</section>
